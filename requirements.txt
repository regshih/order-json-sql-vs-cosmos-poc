# --- API ---------------------------------------------------------------
fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.9
orjson==3.11.3

# --- Azure -------------------------------------------------------------
azure-identity==1.25.0
azure-cosmos==4.14.0
azure-storage-file-datalake==12.20.0
requests==2.32.5          # ARM calls for the Mongo connection string / capabilities

# --- SQL ---------------------------------------------------------------
pyodbc==5.2.0

# --- Mongo (Scenario B) ------------------------------------------------
# Standard supported MongoDB driver, per the brief. Brings `bson` with it,
# which the size tests use to measure ENCODED document size rather than
# source JSON size.
pymongo==4.18.0

# --- Load testing / analysis ------------------------------------------
httpx==0.28.1
locust==2.41.6

# --- Data --------------------------------------------------------------
pandas==2.3.3
pyarrow==21.0.0
deltalake==1.1.4

# --- Tests -------------------------------------------------------------
pytest==8.4.2
pytest-asyncio==1.2.0
python-dotenv==1.1.1
