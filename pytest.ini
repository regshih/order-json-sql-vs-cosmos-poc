[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: requires live Azure resources (SQL / Cosmos / Storage)
    contract: compares API(SQL) with API(Cosmos); requires both backends
    slow: large payload generation
